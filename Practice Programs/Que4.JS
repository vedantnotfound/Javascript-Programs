// JavaScript Program to Calculate the Area of a Triangle

// Formula -> Area of Triangle  A = ½ (b × h) square units

let b = 4;
let h = 6;

let areaOfTriangle = 1/2 * (b * h);

console.log(`b = ${b}\nh = ${h}\nArea of Triangle is ${areaOfTriangle} Square Units `);